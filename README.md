# Door Survey App

Simple React + TypeScript starter app for tracking door-to-door survey activity.

## Getting started

1. Install dependencies:
   `npm install`
2. Start the dev server:
   `npm run dev`
3. Build for production:
   `npm run build`
